loadstring(game:HttpGet(("https://raw.githubusercontent.com/daucobonhi/Ui-Redz-V2/refs/heads/main/UiREDzV2.lua")))()

local Window = MakeWindow({
    Hub = {
        Title = "Bear Hud(Premium) by Quang Huy",
        Animation = "Bản Premium"
    },
    Key = {
        KeySystem = false,
        Title = "Huy Tổng Hợp",
        Description = "Dùng Script Vui Vẻ",
        KeyLink = "https://link4m.com/kjzVaRAh",
        Keys = {""},
        Notifi = {
            Notifications = true,
            CorrectKey = "Hello fen",
            Incorrectkey = "Bản premium có key đâu mà nhập key",
        }
    }
})

-- Tạo nút thu nhỏ có viền
local MinButton = MinimizeButton({
    Image = "http://www.roblox.com/asset/?id=134852113716171",
    Size = {25, 25},
    Color = Color3.fromRGB(255,255,255),
    Corner = true,
    Stroke = true,
    StrokeColor = Color3.fromRGB(255, 0, 0) -- Màu mặc định ban đầu
})

-- Đoạn code xử lý hiệu ứng nháy đổi màu viền (RGB)
task.spawn(function()
    while task.wait() do 
        local hue = tick() % 5 / 5 -- Thay đổi tốc độ bằng cách chỉnh con số 5 (số càng nhỏ đổi màu càng nhanh)
        local color = Color3.fromHSV(hue, 1, 1)
        
        -- Cập nhật màu cho viền của nút
        if MinButton then
            MinButton.StrokeColor = color
        end
    end
end)
